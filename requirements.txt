# Core runtime requirements for code_docking / dockbench (pip)
# Note: GPU users should install the matching torch/torchvision build from https://pytorch.org/get-started/locally/
# Adjust torch/torchvision versions as needed for your CUDA.

# PyTorch stack
torch==2.2.2
pytorch-ignite>=0.4.10

# Scientific stack
numpy<2
scipy>=1.7
scikit-learn>=1.0
pandas>=1.3

# Utils
PyYAML>=5.4
tqdm>=4.62

# Optional but commonly used
matplotlib>=3.4
seaborn>=0.11

mlflow
molgrid
