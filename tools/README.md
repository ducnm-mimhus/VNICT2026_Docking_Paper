# tools/

Đặt binary Linux `gninatyper` tại `tools/gninatyper` (chmod +x) để notebook Colab tự tìm thấy.

Lấy từ Docker: `docker create gnina/gnina:v1.1` → `docker cp <id>:/usr/local/bin/gninatyper ./gninatyper`
